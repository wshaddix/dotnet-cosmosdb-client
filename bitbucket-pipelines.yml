# You can specify a custom docker image from Docker Hub as your build environment.
image: microsoft/dotnet:1.1.2-sdk
pipelines:
  default:
    - step:
        script: # Modify the commands below to build your repository.
          - cd ./src/Client
          - dotnet restore
          - dotnet build --configuration release
          # the tests won't run b/c there is no cosmosdb emulator running in this docker image
          #- dotnet test ./Tests/Tests.csproj
          - dotnet pack --no-build --output nupkgs --configuration release --version-suffix $BITBUCKET_COMMIT
          - cd ./nupkgs
          - dotnet nuget push *.nupkg -s  http://nts-us-south-central-dev-nts-nuget.azurewebsites.net/api/v2/package -k hy942q53h461v130T5T59wticZLst0l6